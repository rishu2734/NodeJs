function accept(sentence){
    return final_line = sentence + " too.";
};

function reject(sentence){
    return final_line = sentence + " my foot.";
};

module.exports.accept = accept;
module.exports.reject = reject;